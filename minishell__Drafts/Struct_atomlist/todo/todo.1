
def.
	tokenlist = tokenize(line, NULL, envlist);

expl.
	- it takes `envlist` as 3nd param (so we need the envlist Struct)
	- 1st param `line` is input received via readline from prompt
	- 2nd param can takes nothing OR ( t_tokenlist *tokenlist )

tokenize

	1 - get raw token
